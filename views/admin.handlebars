
<section  class="container all">
    <div class="form-wrap all ">
        <h1>Admin</h1>
        <br>
        <br>
        <button type="button" onclick="reset(['love'])" class="btn btn-primary">Reset Like</button>
        <br>
        <br>
        <button type="button" onclick="reset(['favorite'])" class="btn btn-primary">Reset Favarite</button>
        <br>
        <br>
        <button type="button" onclick="reset(['love','favorite'])" class="btn btn-primary">Reset Favarite&Like</button>
        <br>
        <br>
        <button type="button" onclick="reset(['love','hate','favorite'])" class="btn btn-primary">Reset Everything</button>
    </div>
</section>
<script>
    if(confirm("Are you sure to Ban this item?")){
      $.ajax({
          url: '/products/banItem',
          type: 'post',
          data: { lotus_id: id },
      }).done(function(data) {
        if (data == "Updated"){
          $('#'+id).attr('display','none');
          $('#'+id).attr('class','hidden');
        }
        loaded();
      });
      } else {
          loaded();

      }
</script>
